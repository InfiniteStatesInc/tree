# PROJECT-PLAN

> 运行状态快照 + 精简历史。新 session 先看「当前状态」。
> **Deadline: 2026-04-22 16:00（今天下午 4 点）**

## 当前状态

- 正在做：用户在 Figma 画 6 帧关键帧 SVG
- 下一步：用户交付 SVG → 组装形变动画 → 加风吹微动 → 交付 HTML
- 上次完成：技术验证（flubber 形变可行性已确认）

---

## 里程碑

- [x] Phase 0：技术验证（SVG 形变实验）
- [ ] Phase 1：关键帧资产准备（用户画图 + 导出）← 进行中
- [ ] Phase 2：形变动画组装
- [ ] Phase 3：风吹微动 + 打磨交付

---

## 活跃任务

### Phase 1：关键帧资产准备

- [ ] T1. 用户在 Figma 画 6 帧关键帧（完成标准：6 帧全部画完）
  - F1 (0s): 种子/破土
  - F2 (2s): 小芽 + 两片叶 ← 已有
  - F3 (4s): 分叉中芽 + 大叶 ← 已有
  - F4 (6s): 小树（主干+枝条雏形）← **需要画**
  - F5 (8s): 中树（枝条展开、树冠初现）← **需要画**
  - F6 (10s): 满开大树 + 粉色花冠 ← 已有雏形，需补完
- [ ] T2. 每帧拆层导出 SVG（完成标准：每帧的树干/枝条/叶子/花冠各自独立 SVG）
  - 每帧都要导出一个「整棵.svg」（所有部件保持原位的合并版，用于自动坐标提取）
  - 各部件单独导出（树干.svg、左叶.svg、右叶.svg 等）
  - **命名规则**：`F1-树干.svg`、`F3-左叶.svg` 这样，帧号+部件名

### Phase 2：形变动画组装

- [ ] T3. 接收 SVG，解析坐标，建立 6 帧形变链（完成标准：拖滑条能看到连续形变）
  - 用 flubber 建立相邻帧之间的 path interpolator
  - 树干形变链：F1干→F2干→F3干→F4干→F5干→F6干
  - 叶子形变链：同理
  - 花冠：后期帧出现时 scale-in
- [ ] T4. 映射到天数控制（完成标准：点击 Day 1-5 按钮 + 拖滑条都能控制）

### Phase 3：风吹微动 + 打磨

- [ ] T5. 给每个静止状态加风吹微动动画（完成标准：停在任何一天都有微风摇摆感）
  - 枝条：CSS `@keyframes` 微旋转，transform-origin 在分叉点
  - 叶子：轻微摇摆 + 微缩放
  - 花冠：缓慢轻微漂浮
  - 不同部件的动画周期错开，避免机械感
- [ ] T6. 背景 + 地面（完成标准：米色背景 + 底部泥土/地面元素）
- [ ] T7. 最终检查交付（完成标准：用户确认可以展示）

---

## 已锁定决策

- 技术方案：SVG Path Morphing (flubber)，每帧拆层，代码做形变+微动
- 关键帧数量：6 帧
- 交付物：单个 HTML 文件，可直接用浏览器打开
- 背景：米色纯色底，树"放在地上"
- 不做：浮空岛（留给后续版本）

---

## 约束

- 时间：今天下午 4 点前要能展示
- 技术：flubber 要求 SVG path 数据，不能是位图
- 不可改动：用户手绘的造型不能被代码修改

---

## 关键帧部件拆分规范

每帧需要导出的部件（视实际造型调整）：

| 部件类型 | 说明 | 命名示例 |
|---------|------|---------|
| 树干 | 主干+所有枝条作为一个整体 | `F1-树干.svg` |
| 叶子（逐个） | 每片叶子单独导出 | `F2-叶左.svg`、`F2-叶右.svg` |
| 花冠（逐个） | 每团花冠单独导出 | `F6-花冠A.svg` |
| 整棵树 | 所有部件保持原位合并 | `F3-整棵.svg` |

**要点**：
- 6 帧的树干底部（根部）尽量对齐同一水平线，方便形变时锚定
- 相邻帧之间造型不要跳太大

---

## 已完成阶段

### Phase 0：技术验证（4/21 - 4/22）
在 `~/Projects/实验/prayer-tree-growth.html` 完成了 SVG 形变实验。验证了 flubber 路径插值对用户手绘 SVG 部件的可行性。确认了分层渐进揭示 + 路径形变的技术方案。从 Figma 通过 Vibma MCP 直接导出了 12 个测试部件并成功运行。
